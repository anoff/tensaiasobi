# Antigravity CLI Output Rules

- **Artifact Generation**: Whenever you generate a file, document, plan, or markdown artifact, ALWAYS save it directly to the current working directory (`./`) or a relevant visible subdirectory within this project.
- **No Brain Sandbox**: NEVER write generated files to the internal `~/.gemini/antigravity-cli/brain...` directories. All files must be accessible in the local workspace.